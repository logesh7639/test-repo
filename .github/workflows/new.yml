name: Concurrency Demo

on:
  workflow_dispatch:

concurrency:
  group: demo
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Testing concurrency..."
          sleep 30
          echo "Completed"
