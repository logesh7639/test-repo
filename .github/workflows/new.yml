name: Environment Variables

on:
  workflow_dispatch:

env:
  APP_NAME: GitHub Actions Lab
  VERSION: 1.0

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Print Variables
        run: |
          echo "Application: $APP_NAME"
          echo "Version: $VERSION"
