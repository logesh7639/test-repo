name: Test Variables and Secrets

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print Variable
        run: echo "Variable = ${{ vars.TEST_VAR }}"

      - name: Print Secret
        run: echo "Secret = ${{ secrets.TEST_SECRET }}"
